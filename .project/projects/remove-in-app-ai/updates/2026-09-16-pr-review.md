# PR review repairs

Codex completed review of PR #18 at `80c8ed7` and reported two valid omissions. Pricing now advertises agent proposals and inline diff review, without the removed quality scoring or a duplicate Personal feature. Local search again recognizes the existing aliases for all seven retained Personal settings destinations. The Company guard and settings anchors are unchanged.

Added a regression test that searches by permissions, repo, backup, remove account, email, connect account, and restore and checks the highest-ranked settings destination. Refreshed the brand audit fingerprints after reviewing the unchanged compatibility identifiers in the two edited components.

Verification in an isolated worktree: 175 tests, 167 passed, 8 opt-in database tests skipped; TypeScript passed; lint passed with the same six existing warnings; brand audit passed; production build passed with `NEXT_PUBLIC_SITE_URL=http://localhost:3000`. The initial build lacked that required variable and was rerun after configuring it. Prior browser and runtime evidence remains applicable; these repairs only change search aliases and pricing copy. CI and Codex review must run again on the repair commit before merge.
